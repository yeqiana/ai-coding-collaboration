interface:
  display_name: "AI Coding Collaboration"
  short_description: "Classify, implement, validate, and summarize coding tasks."
  default_prompt: "Use $ai-coding-collaboration to classify this coding task, implement with minimal diff, validate it, and summarize the result."

policy:
  allow_implicit_invocation: true
